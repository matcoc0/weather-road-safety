def main(
    dataset: str,
    year: int,
    department: str,
) -> None:
    print(
        f"Silver pipeline: "
        f"dataset={dataset}, "
        f"year={year}, "
        f"department={department}"
    )